{
  "name": "disbursement-tracker",
  "private": true,
  "version": "1.0.0",
  "description": "A simple, single-file disbursement tracking application.",
  "scripts": {
    "dev": "vite --port 3000 --host 0.0.0.0",
    "build": "vite build",
    "lint": "echo 'No linting configured'"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  },
  "dependencies": {
    "firebase": "^12.10.0"
  }
}
